<h1> All Products </h1>
<div id='wrapper'>

  <% @products.each do |product| %>
  <a href='/products/<%= product.id %>'>
  <div class='product'>
    <div class='thumb'>
      <img src='<%= procuct['url'] %>' />
    </div>
    <div class='caption'>
      <%= product.title %> for only &#36;<%= product.price %> !
    </div>
  </div>
  </a>
  <% end %>

</div>